/**
 * Terminal card padding constants
 * Used for consistent padding across Terminal page cards (Total, Earned, Yield Multiplayer)
 */
export const TERMINAL_CARD_PADDING = {
  mobile: "p-7", // 28px
  desktop: "md:p-7", // 28px on desktop
  className: "p-7", // Same padding for both mobile and desktop
} as const;

